require('scrollbar').setup({
    show_in_active_only = true,
    hide_if_all_visible = true,
    handlers = {
        gitsigns = true,
        search = true
    },
    marks = {
        Search = { color = '#85d3f2' }
    }
})

